<?xml version="1.0" encoding="utf-8"?>
<odoo>
  <record id="ocr_document_search" model="ir.ui.view">
    <field name="name">ocr.document.search</field>
    <field name="model">ocr.document</field>
    <field name="arch" type="xml">
      <search string="OCR Documents">

        <field name="name"/>
        <field name="document_type"/>
        <field name="status"/>

        <filter name="draft" string="Draft" domain="[('status','=','draft')]"/>
        <filter name="processing" string="Processing" domain="[('status','=','processing')]"/>
        <filter name="done" string="Done" domain="[('status','=','done')]"/>
        <filter name="failed" string="Failed" domain="[('status','=','failed')]"/>

        <group expand="0" string="Group By">
          <filter name="group_by_state" string="State" context="{'group_by':'status'}"/>
          <filter name="group_by_type" string="Document Type" context="{'group_by':'document_type'}"/>
        </group>

      </search>
    </field>
  </record>
</odoo>

<?xml version="1.0" encoding="UTF-8"?>
<odoo>

    <record id="action_ocr_document_upload_invoice" model="ir.actions.act_window">
        <field name="name">Upload Invoice</field>
        <field name="res_model">ocr.document</field>
        <field name="view_mode">form</field>
        <field name="target">current</field>
        <field name="context">{'default_document_type': 'invoice'}</field>
    </record>

    <record id="action_ocr_document_upload_receipt" model="ir.actions.act_window">
        <field name="name">Upload Receipt</field>
        <field name="res_model">ocr.document</field>
        <field name="view_mode">form</field>
        <field name="target">current</field>
        <field name="context">{'default_document_type': 'receipt'}</field>
    </record>

    <record id="action_ocr_document_list" model="ir.actions.act_window">
        <field name="name">Document History</field>
        <field name="res_model">ocr.document</field>
        <field name="view_mode">tree,form</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Upload your first document!
            </p>
        </field>
    </record>

</odoo>
